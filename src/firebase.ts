// Firebase removido — sistema migrado para MySQL via REST API
// Arquivo mantido para evitar erros de import em componentes não migrados
export const auth = null;
export const db = null;
export const storage = null;
